var React = require('react');

var TimeSlot = React.createClass({
    render: function() {
        return (
            <div className="time-slot">
                <h2>TimeSlot</h2>
            </div>
        );
    }
});

module.exports = TimeSlot;